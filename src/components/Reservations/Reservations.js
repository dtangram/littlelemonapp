import Counter from './Counter/Counter';

const Reservation = () => {
    return (
        <main className="container">
            <h1>Reservations</h1>
            <section>
                <Counter />
            </section>
        </main>
    );
}

export default Reservation;